services:
  mc:
    image: itzg/minecraft-server
    environment:
      EULA: "true"
    ports:
      - "36085:25565"
    volumes:
      - minesdata:/data
    stdin_open: true
    tty: true
    restart: unless-stopped
volumes:
  data: {}
